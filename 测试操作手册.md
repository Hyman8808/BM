# 📘 图灵 1.0 硬件测试框架 - 操作手册 (V1.3)

本手册旨在指导测试人员如何快速配置并运行基于 **WebSocket (Socket)**、**AI-WIFI (HTTP)** 及 **指尖查词** 协议的自动化测试套件。

---

## 1. 项目结构说明

- `picbook_*.py`: 绘本 1.0 WebSocket 协议测试脚本。
- `http_picbook_*.py`: 绘本 AI-WIFI (HTTP) 协议测试脚本。
- `fingertip_*.py`: 指尖查词 (OpenSocket) 协议测试脚本。
- `config_*.json`: [核心] 各协议对应的独立参数配置文件。
- `log*/`: 各测试任务生成的详细事务请求/响应日志。
- `*.xlsx`: 自动生成的 BI 级可视化性能报告。

---

## 2. 配置管理 (Config Management)

项目采用 **“JSON配置文件 + 命令行参数覆盖”** 的方式，确保配置的灵活性。

### 2.1 配置文件说明
修改脚本同目录下的相关 `.json` 文件即可更改默认测试环境：

| 配置文件 | 对应业务 | 关键参数 |
| :--- | :--- | :--- |
| `config_picbook.json` | 绘本 (WebSocket) | API_KEY, SECRET, DEVICE_ID, CAMERA_ID |
| `config_http.json` | 绘本 (AI-WIFI/HTTP) | API_KEY, SECRET, RAW_DEVICE_ID | 
| `config_fingertip.json` | 指尖查词 (Socket) | API_KEY, SECRET, DEVICE_ID, CAMERA_ID |

### 2.2 命令行参数覆盖 (CLI Overrides)
无需修改文件，可在运行时直接通过参数指定（**优先级最高**）：
- `--ak`: 覆盖 API Key
- `--secret`: 覆盖 Secret Key
- `--uid`: 覆盖设备 ID (UID)
- `--url`: 覆盖请求 URL (WebSocket/HTTP)

---

## 3. 运行指南

### 3.1 绘本协议测试
```powershell
# [批量测试] 自动识别 绘本图片/ 目录下所有文件并生成报表
python picbook_batch_test.py

# [带参数运行]
python picbook_batch_test.py --ak XXX --uid YYY

# [单步调试] 仅测试封面及第一张内页
python picbook_single_test.py
```

### 3.2 绘本 HTTP (AI-WIFI) 测试
```powershell
# [批量测试]
python http_picbook_batch_test.py

# [参数覆盖]
python http_picbook_batch_test.py --uid ai11223344556677
```

### 3.3 指尖查词测试
```powershell
# [批量测试] 自动识别 指尖查词图片/ 目录下所有文件
python fingertip_batch_test.py
```

---

## 4. 结果采集与运维

### 4.1 性能报表 (.xlsx)
测试完成后，根目录会自动生成 `XXXX_Performance_时间戳.xlsx` 文件：
- **数据看板**：包含请求成功率、平均响应延时、波动柱状图。
- **测试详情**：记录每一张图片的耗时、OCR 结果及关联日志路径。

### 4.2 事务日志 (Logs)
所有原始数据保存在 `log_*/` 目录中：
- 用于排查服务端返回的具体错误码（如 `4003` 权限不足、`1018` 图像质量等）。

### 4.3 自动清理机制
- **报表清理**：脚本运行时会自动删除 **15分钟以上** 的旧 `.xlsx`。
- **日志清理**：脚本启动时会检测 **24小时以上** 的旧日志，并提示您是否确认清理。

---

## 5. 常见问题排查 (FAQ)

- **Q: 为什么运行提示 ModuleNotFoundError: 'xlsxwriter'?**
  - A: 脚本会自动尝试 pip 安装，若失败请手动运行 `pip install xlsxwriter pycryptodome websockets requests`。
- **Q: 为什么 HTTP 模式报 uid illegal value?**
  - A: 请确保在 `config_http.json` 中配置的 `RAW_DEVICE_ID` 是 **16位** 的数字/字母组合。脚本会自动按照 AES 完成协议转换。
